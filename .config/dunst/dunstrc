[global]
# Position at bottom-center of the screen
origin = top-center
# Offset can be adjusted if needed (horizontal x vertical)
offset = (0, 32)

# Notification width (dynamic up to 500px)
width = (0, 500)
# Max height of a notification
height = (0, 100)

# Font with Iosevka
font = Iosevka 10

# Frame and corner radius for minimal look
frame_width = 0
corner_radius = 6

# Transparency (0 = opaque, adjust if you want)
transparency = 0

# Follow none (fixed position)
follow = none

[urgency_low]
# Gruvbox dark theme colors for low urgency
background = "#282828"
foreground = "#ebdbb2"
frame_color = "#3c3836"

[urgency_normal]
# Gruvbox dark theme colors for normal urgency
background = "#282828"
foreground = "#ebdbb2"
frame_color = "#458588"

[urgency_critical]
# Gruvbox dark theme colors for critical urgency
background = "#cc241d"
foreground = "#fbf1c7"
frame_color = "#fb4934"
